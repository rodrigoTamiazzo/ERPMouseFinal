<h2><%= @page_title = "Excluir requisicao" %></h2><hr />

<p class="red-text strong" style="font-size:16px">Você tem certeza que deseja deletar permanentemente esta requisicao?</p>
<%= form_for @material_prova, :html => {:class => "form-horizontal"} do |f| -%>

  <%= render(:partial => 'form', :locals => {:f => f, :readonly => true}) %>    
  <div class="control-group">
	  <div class="controls">
		  <%= button_to "Excluir requisicao", material_provas_path(@banner), :method => 'delete', :class => "btn btn-small dark-blue" %>
	  </div>
  </div>
<% end %>

<%= link_to 'Voltar', material_provas_path, :class => "btn btn-small light-blue inverse" %>